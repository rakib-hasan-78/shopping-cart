import React from 'react';
import Nav from './../Nav/Nav';

const Header = () => {
    return (
        <header className='w-full h-auto'>
            <main className='main'>
            <Nav />
            </main>
        </header>
    );
};

export default Header;